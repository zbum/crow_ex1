# Crow API Server Configuration

# Database Configuration
database:
  host: "127.0.0.1"
  port: 3306
  username: "root"
  password: "test"
  database: "test"

# Server Configuration
server:
  host: "0.0.0.0"
  port: 8080
  threads: 10
